config:
  target: "https://dzencode.com/en/"
  phases:
    - duration: 1000
      arrivalRate: 10
      name: Artillery First Test
scenarios:
  - name: Getting a resource
    flow:
      - get:
          url: "{{ target }}get"